# Frontend

```powershell
cd frontend
npm install
npm run dev
```

This is intentionally only a Next.js TypeScript application shell. It has no catalog UI, recommendation flow, or compatibility interface yet.
