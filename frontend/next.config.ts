import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the development-only Next.js badge from the product interface.
  devIndicators: false,
};

export default nextConfig;
